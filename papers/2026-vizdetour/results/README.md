# Generated evidence

This directory is populated by `reproduce.sh` and GitHub Actions.

The CI uploads the artifact-audit JSON plus live Matplotlib seed/mutant images and `report.json` files as workflow artifacts. Generated outputs are evidence, not source-of-truth fixtures; the pinned scripts and artifact commit define the reproduction.
